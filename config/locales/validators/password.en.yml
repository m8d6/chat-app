en:
  activerecord:
    errors:
      messages:   
        no_special_char: "must include at least one special character"
        no_digit: "must include at least one digit"
        no_uppercase: "must include at least one uppercase letter"
        no_lowercase: "must include at least one lowercase letter"
